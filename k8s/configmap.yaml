apiVersion: v1
kind: ConfigMap
metadata:
  name: dostt-rewards-config
  namespace: default
data:
  PORT: "3001"
  DB_ADAPTER: "postgres"
  CYCLE_DAYS: "30"
  GO_LIVE_DATE: ""   # Set to ISO timestamp when rewards go live, e.g. "2026-01-01T00:00:00+05:30"
